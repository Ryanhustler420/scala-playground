version: "3.9"

services:
  scala-notebook:
    build: .
    container_name: scala-almond-notebook
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/home/jovyan/work
    environment:
      - JUPYTER_ENABLE_LAB=yes
    restart: unless-stopped
